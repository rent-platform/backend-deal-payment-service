spring:
  datasource:
    url: jdbc:postgresql://${PG_HOST:postgres}:${PG_PORT:5432}/${PG_DATABASE:deal_db}
    username: ${PG_USER:postgres}
    password: ${PG_PASSWORD:12345}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false

  flyway:
    enabled: true
    locations: filesystem:/app/database/migrations

app:
  user-service-url: http://user-service:8081
  swagger:
    gateway-url: http://localhost:8180
    deal-payment-service-url: http://localhost:8183

server:
  port: 8083